[project]
name = "recxchange-friction"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.34",
  "pydantic>=2.10",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
